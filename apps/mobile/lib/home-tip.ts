// The home greeting + personal note now live in @bowlkollen/core so web + native
// share them verbatim. Re-exported here so existing `@/lib/home-tip` imports keep
// working unchanged.
export { greetingFor, homeNote, type HomeNote } from '@bowlkollen/core';
