// Curated competitions list — moved to @bowlkollen/core so web + native render the
// same data (see packages/core/src/competitions.ts). Re-exported for existing imports.
export { TAVLINGAR, getLiveCompetitions, type Tavling } from '@bowlkollen/core'
